import {trainingSessions} from "./state";

export type DateType = {
    dateId: string
}
export type TimeType = {
    timeId: string
    timeTitle: string
}
export type TrainingType = {
    exerciseId: string
    exerciseName: string
    description: string
}
